(defwidget time_module []
    (box
      :class "text"
      :space-evenly false
      :orientation "h"
      :tooltip {formattime(EWW_TIME, "%c")}
      (label :text {formattime(EWW_TIME, "%H:%M")})))
