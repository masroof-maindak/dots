#!/bin/sh

if [ -f /etc/os-release ]; then
    . /etc/os-release
    OS=$NAME
fi
KERNEL=$(uname -r)
KERNEL="${KERNEL%?-?}"
echo "  ／l、     "
echo "（ﾟ､ ｡７    ${OS}"  
echo " l  ~ ヽ    ${KERNEL}"
echo " じしf_,)ノ"
echo ""
